{
  "name": "My_Project_Here",
  "version": "1.0.0",
  "private": true,
  "description": "Description of project here",
  "scripts": {
    "setup": "cd graphile-starter-project && yarn setup",
    "setup:docker": "cd .devcontainer && yarn devcontainer:setup",
    "dev": "cd graphile-starter-project && yarn dev",
    "dev:docker": "bash -c \". ./graphile-starter-project/.env && cd .devcontainer && yarn devcontainer:up && yarn devcontainer:dev \"",
    "PLACEHOLDER-WIP":"==================================",
    "docker-run-once": "docker-compose -f ./.devcontainer/docker-compose.builder.yml run --rm base",
    "docker-exec-dev": "docker-compose -f ./.devcontainer/docker-compose.yml down && docker-compose -f ./.devcontainer/docker-compose.yml up -d dev && docker-compose -f ./.devcontainer/docker-compose.yml exec dev ",
    "vscode": "bash -c \". ./graphile-starter-project/.env && code .\""
  },
  "author": "Benjie Gillam <code@benjiegillam.com>",
  "license": "SEE LICENSE IN LICENSE.md",
  "dependencies": {},
  "devDependencies": {
    "cross-env": "^6.0.3"
  }
}
