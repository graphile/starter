{
  "name": "My_Project_Here",
  "version": "1.0.0",
  "private": true,
  "description": "Description of project here",
  "scripts": {
    "setup": "cd .devcontainer && yarn devcontainer:setup",
    "start": "yarn dev",
    "dev": "bash -c \". ./graphile-starter-app/.envvar && cd .devcontainer && yarn devcontainer:dev \"",
    "reset": "cd .devcontainer && yarn reset",
    "/* WIP/PLACEHOLDER/TOOLS */":"//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////",
    "work-in-devcontainer": "bash -c \". ./graphile-starter-app/.envvar && cd .devcontainer && yarn devcontainer:up && cd .. && yarn attach-to-devcontainer\"",
    "attach-to-devcontainer": "bash -c \". ./graphile-starter-app/.envvar && cd .devcontainer && yarn docker-attach-dev bash \"",
    "vscode": "bash -c \". ./graphile-starter-app/.envvar && code .\""
  },
  "author": "Benjie Gillam <code@benjiegillam.com>",
  "license": "SEE LICENSE IN LICENSE.md",
  "dependencies": {},
  "devDependencies": {
    "cross-env": "^6.0.3"
  }
}
