#import "./EmailsForm_UserFragment.graphql"

mutation AddEmailMutation($email: String!) {
  createUserEmail(input: { userEmail: { email: $email } }) {
    user {
      userEmails {
        nodes {
          id
          ...EmailsForm_UserFragment
        }
      }
    }
  }
}
