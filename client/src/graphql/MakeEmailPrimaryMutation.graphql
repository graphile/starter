mutation MakeEmailPrimaryMutation($emailId: Int!) {
  makeEmailPrimary(input: { emailId: $emailId }) {
    user {
      id
      userEmails {
        nodes {
          id
          isPrimary
        }
      }
    }
  }
}
