mutation UnlinkUserAuthentication($id: Int!) {
  deleteUserAuthentication(input: { id: $id }) {
    user {
      id
      userAuthenticationsList {
        id
        identifier
        service
        createdAt
      }
    }
  }
}
