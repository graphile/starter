mutation Register(
  $username: String!
  $password: String!
  $email: String!
  $name: String
) {
  register(
    input: {
      username: $username
      password: $password
      email: $email
      name: $name
    }
  ) {
    user {
      id
      username
      name
    }
  }
}
