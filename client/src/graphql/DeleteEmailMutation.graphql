#import "./EmailsForm_UserEmailFragment.graphql"

mutation DeleteEmailMutation($emailId: Int!) {
  deleteUserEmail(input: { id: $emailId }) {
    user {
      id
      userEmails {
        nodes {
          id
          ...EmailsForm_UserEmailFragment
        }
      }
    }
  }
}
