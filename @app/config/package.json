{
  "name": "@app/config",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:essential": "yarn run build",
    "watch": "tsc -w"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
