{
  "name": "@app/migrations",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "watch": "graphile-migrate watch",
    "commit": "graphile-migrate commit",
    "uncommit": "graphile-migrate uncommit",
    "dump": "graphile-migrate migrate && graphile-migrate reset --shadow && graphile-migrate migrate --shadow --force",
    "wipe-if-demo": "./scripts/wipe-if-demo"
  },
  "dependencies": {
    "graphile-migrate": "^0.0.17"
  },
  "devDependencies": {
    "lodash": "^4.17.15"
  }
}
