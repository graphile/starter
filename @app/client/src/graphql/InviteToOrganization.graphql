mutation InviteToOrganization(
  $organizationId: Int!
  $email: String
  $username: String
) {
  inviteToOrganization(
    input: {
      organizationId: $organizationId
      email: $email
      username: $username
    }
  ) {
    clientMutationId
  }
}
