fragment OrganizationPageOrganization on Organization {
  id
  name
  slug
  currentUserIsOwner
  currentUserIsBillingContact
}

query OrganizationPage($slug: String!) {
  ...SharedLayout_Query
  organizationBySlug(slug: $slug) {
    id
    ...OrganizationPageOrganization
  }
}
